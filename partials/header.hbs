{{#is "home"}}
    <header id="site-head">
        {{#if @blog.logo}}
            <p style="text-align:center;"><img class="bloglogo" src="{{@blog.logo}}"/></p>
        {{/if}}

        <h1 class="blog-title"><a href="{{@blog.url}}">{{@blog.title}}</a></h1>

        <h2 class="blog-description">{{@blog.description}}</h2>
        {{navigation}}
    </header>
{{else}}
    <header id="site-head" class="internal">
        <h1 class="blog-title">
            {{#if @blog.logo}}
                <img class="bloglogo" src="{{@blog.logo}}"/>
            {{/if}}
            <a href="{{@blog.url}}">{{@blog.title}}</a>
        </h1>

        {{!-- <h2 class="blog-description">{{@blog.description}}</h2> --}}
        {{navigation}}
    </header>
{{/is}}


